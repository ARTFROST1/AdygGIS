# Исправление отображения полей в карточке места

**Дата:** 11 января 2026  
**Проблема:** В Kotlin версии не отображаются поля: сезон работы, стоимость, продолжительность, лучшее время, секции Удобства и Метки

## 🔍 Диагностика

### ✅ Проверено - всё в порядке:

1. **База данных Supabase:**
   - Все 33 места имеют заполненные поля
   - `operating_season`: 33/33 ✓
   - `price_info`: 33/33 ✓
   - `duration`: 33/33 ✓
   - `best_time_to_visit`: 33/33 ✓
   - `amenities`: 33/33 ✓
   - `tags`: 33/33 ✓

2. **Data Models:**
   - `AttractionEntity` - все поля присутствуют ✓
   - `Attraction` (domain) - все поля присутствуют ✓
   - `AttractionDto` - все поля присутствуют ✓

3. **Маппинг:**
   - `AttractionMapper.toDomainModel()` - корректно переносит все поля ✓

4. **UI Components:**
   - `ModernAttractionInfo` - отображает все поля ✓
   - `ModernAmenitiesSection` - правильно реализован ✓
   - `ModernTagsSection` - правильно реализован ✓

5. **Integration:**
   - `AttractionBottomSheet` - все компоненты подключены ✓

### ❓ Возможные причины

Если поля НЕ отображаются при открытии места, возможны следующие причины:

1. **Локальная база данных не синхронизирована**
   - Данные в Room БД устарели
   - Последняя синхронизация была давно

2. **Приложение не пересобрано**
   - Изменения в коде не применены
   - Нужна пересборка

3. **Кэш изображений/данных**
   - Старые данные в кэше

## ✅ Решение

### Шаг 1: Принудительная синхронизация

Добавьте принудительную синхронизацию при запуске:

```kotlin
// В MainActivity или MapScreen
LaunchedEffect(Unit) {
    // Принудительная синхронизация при запуске
    dataSyncManager.syncNow(forceFullSync = true)
}
```

### Шаг 2: Очистка и пересборка

```bash
# В терминале проекта
./gradlew clean
./gradlew assembleDebug

# Или в Android Studio
Build → Clean Project
Build → Rebuild Project
```

### Шаг 3: Очистка данных приложения на устройстве

На Android устройстве:
1. Settings → Apps → AdygGIS
2. Storage → Clear Data
3. Переустановить приложение

### Шаг 4: Проверка логов

Добавьте временные логи для проверки данных:

```kotlin
// В ModernAttractionInfo
Log.d("AttractionInfo", """
    Attraction data:
    - operatingSeason: ${attraction.operatingSeason}
    - priceInfo: ${attraction.priceInfo}
    - duration: ${attraction.duration}
    - bestTimeToVisit: ${attraction.bestTimeToVisit}
    - amenities: ${attraction.amenities.size}
    - tags: ${attraction.tags.size}
""".trimIndent())
```

## 📋 Контрольный список

- [ ] Пересобрать проект (Clean + Rebuild)
- [ ] Удалить приложение с устройства
- [ ] Установить новую сборку
- [ ] Запустить приложение с интернетом (для синхронизации)
- [ ] Дождаться завершения синхронизации
- [ ] Открыть любое место
- [ ] Проверить отображение всех полей

## 🎯 Ожидаемый результат

После выполнения шагов в карточке места должны отображаться:

**Основная информация:**
1. 📍 Адрес
2. 🧭 Как добраться
3. ⏰ Время работы
4. ☀️ Сезон работы
5. 💰 Стоимость
6. ⏱️ Продолжительность
7. 📅 Лучшее время

**Контакты:**
1. 📞 Телефон
2. ✉️ Email
3. 🌐 Сайт

**Дополнительно:**
1. 🎯 Удобства (двухколоночный grid)
2. 🏷️ Метки (зелёные теги с градиентом)

## 🔧 Дополнительная диагностика

Если проблема сохраняется, проверьте:

### 1. Синхронизация работает?

```kotlin
// Проверьте логи SyncService
adb logcat | grep "Sync"
```

### 2. Данные в Room БД?

```kotlin
// В DataSyncManager или Repository
val attractions = attractionDao.getAllAttractions()
attractions.forEach { attraction ->
    Log.d("DB", "ID: ${attraction.id}")
    Log.d("DB", "Operating season: ${attraction.operatingSeason}")
    Log.d("DB", "Price: ${attraction.priceInfo}")
    Log.d("DB", "Duration: ${attraction.duration}")
    Log.d("DB", "Best time: ${attraction.bestTimeToVisit}")
    Log.d("DB", "Amenities: ${attraction.amenities}")
    Log.d("DB", "Tags: ${attraction.tags}")
}
```

### 3. Маппинг работает?

```kotlin
// В AttractionMapper
fun AttractionEntity.toDomainModel(): Attraction {
    val result = Attraction(
        // ... остальные поля
        operatingSeason = operatingSeason,
        duration = duration,
        bestTimeToVisit = bestTimeToVisit,
        priceInfo = priceInfo,
        amenities = amenities,
        tags = tags
    )
    
    Log.d("Mapper", """
        Mapped attraction:
        - operatingSeason: ${result.operatingSeason}
        - priceInfo: ${result.priceInfo}
        - duration: ${result.duration}
        - bestTimeToVisit: ${result.bestTimeToVisit}
        - amenities: ${result.amenities.size}
        - tags: ${result.tags.size}
    """.trimIndent())
    
    return result
}
```

## 📝 Примечания

- Все изменения в коде уже внесены
- Проблема скорее всего в устаревших данных
- После чистой установки всё должно работать
- Синхронизация при первом запуске обязательна
