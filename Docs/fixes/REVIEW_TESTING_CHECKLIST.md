# Чеклист тестирования системы отзывов

**Дата:** 2026-01-07  
**Цель:** Проверка правильной работы отображения отзывов пользователя

## Быстрый тест (5 минут)

### ✅ Тест 1: Отправка отзыва
1. Войти в аккаунт
2. Открыть любую достопримечательность
3. Нажать "Оставить отзыв"
4. Заполнить: оценка 5★, текст "Тестовый отзыв"
5. Нажать "Отправить"

**Ожидается:**
- ✅ Модальное окно закрылось
- ✅ **СРАЗУ** появилась секция "Ваш отзыв"
- ✅ В секции виден отправленный отзыв
- ✅ Над отзывом бейдж "🟡 На модерации"
- ✅ Отзыв НЕ появился в списке "Отзывы других пользователей"

### ✅ Тест 2: Обновление данных
1. Не выходя из приложения, открыть Supabase Studio
2. В таблице `reviews` найти только что созданный отзыв
3. Изменить `status` с `pending` на `approved`
4. В приложении потянуть вниз (Pull to Refresh)

**Ожидается:**
- ✅ Отзыв остался в секции "Ваш отзыв"
- ✅ Бейдж "На модерации" **исчез**
- ✅ Отзыв **НЕ** дублируется в списке "Отзывы других пользователей"

### ✅ Тест 3: Проверка для других пользователей
1. Выйти из аккаунта (или открыть на другом устройстве)
2. Открыть ту же достопримечательность

**Ожидается:**
- ✅ Секция "Ваш отзыв" НЕ отображается
- ✅ Отзыв виден в списке "Отзывы других пользователей"

## Полный тест (15 минут)

### Тест 4: Отклонение отзыва
1. Войти в аккаунт
2. Отправить новый отзыв
3. В Supabase изменить `status` на `rejected`
4. Установить `rejection_reason` = "Содержит спам"
5. В приложении обновить данные

**Ожидается:**
- ✅ Отзыв остался в секции "Ваш отзыв"
- ✅ Бейдж "🔴 Отклонён"
- ✅ Отзыв НЕ виден другим пользователям

### Тест 5: Множественные отзывы
1. Пользователь 1: отправить отзыв для достопримечательности А
2. Пользователь 2: отправить отзыв для достопримечательности А
3. Админ: одобрить оба отзыва
4. Пользователь 1: открыть достопримечательность А

**Ожидается:**
- ✅ Секция "Ваш отзыв" содержит отзыв пользователя 1
- ✅ Секция "Отзывы других пользователей" содержит отзыв пользователя 2
- ✅ Отзыв пользователя 1 НЕ дублируется

### Тест 6: Реакции на отзывы
1. Войти как пользователь 1
2. Открыть достопримечательность с отзывами других пользователей
3. Нажать "👍" на отзыве другого пользователя
4. Проверить секцию "Ваш отзыв"

**Ожидается:**
- ✅ Секция "Ваш отзыв" осталась неизменной
- ✅ Счетчик лайков в отзыве другого пользователя увеличился

## Проверка логов

Включить фильтр логов Timber по тегу `ReviewViewModel`:

```
adb logcat -s ReviewViewModel
```

### После отправки отзыва должно быть:

```
D/ReviewViewModel: Successfully submitted review, status=pending
D/ReviewViewModel: User own reviews count after submit: 1
```

### При загрузке страницы:

```
D/ReviewViewModel: Loaded user own reviews, count=1, statuses=[pending]
```

### При обновлении (после одобрения):

```
D/ReviewViewModel: Loaded user own reviews, count=1, statuses=[approved]
```

## Критические проверки

### ❌ НЕ должно быть:
- Дублирования отзыва в обеих секциях
- Исчезновения отзыва после отправки
- Отображения бейджа для `approved` отзывов
- Отзыва пользователя в списке "Отзывы других пользователей"

### ✅ Должно быть:
- Мгновенное появление отзыва после отправки
- Правильное отображение статуса
- Разделение на две секции
- Корректная фильтрация

## Известные ограничения

1. **Один отзыв на достопримечательность** - пользователь не может оставить второй отзыв (проверка на уровне БД)
2. **Только авторизованные пользователи** могут оставлять отзывы
3. **Статус по умолчанию** - все новые отзывы получают `status = 'pending'`

## Быстрая диагностика проблем

### Отзыв не появляется после отправки
**Причина:** Не обновился `_userOwnReviews` в ViewModel  
**Решение:** Проверить логи `User own reviews count after submit`

### Отзыв дублируется
**Причина:** Не работает фильтрация  
**Решение:** Проверить `excludeUserId` в `refreshApprovedReviews`

### Бейдж отображается для approved отзыва
**Причина:** Неправильное условие в UI  
**Решение:** Проверить `if (review.status != null && review.status != "approved")`

### Отзыв исчез после обновления
**Причина:** Проблема с API или RLS политиками  
**Решение:** Проверить таблицу `reviews` в Supabase, проверить `getUserOwnReviews` endpoint

## Контакты

При обнаружении проблем:
1. Собрать логи Timber
2. Сделать скриншоты UI
3. Проверить данные в Supabase Studio
4. Открыть issue с подробным описанием
