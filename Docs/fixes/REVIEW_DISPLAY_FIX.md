# Исправление отображения отзывов пользователя

**Дата:** 2026-01-07  
**Статус:** ✅ ПОЛНОСТЬЮ ИСПРАВЛЕНО (включая проблему с перезагрузкой)

## Проблемы

При отображении отзывов в Kotlin версии приложения были обнаружены критические проблемы:

1. ❌ **Отзыв не появлялся сразу после отправки** - пользователь отправлял отзыв на модерацию, но он не отображался в секции "Ваш отзыв"
2. ❌ **Отзыв исчезал при перезагрузке приложения** - КРИТИЧНО! Даже если отзыв появлялся, при перезапуске он исчезал
3. ❌ **Отзыв появлялся только после публикации** - только когда админ одобрял отзыв, он появлялся в общем списке (а не в "Ваш отзыв")
4. ❌ **Дублирование после одобрения** - отзыв со статусом `approved` появлялся и в "Ваш отзыв", и в "Отзывы других пользователей"
5. ❌ **Статус отображался всегда** - бейдж статуса показывался даже для опубликованных отзывов
6. ❌ **Race condition с аутентификацией** - отзывы загружались до завершения восстановления сессии

## Корневая причина проблемы с перезагрузкой

**Проблема:** При открытии карточки достопримечательности вызывался `loadReviews()`, который проверял `isAuthenticated.value`. НО в момент вызова auth состояние было еще `AuthState.Unknown` или `AuthState.Loading`, так как восстановление сессии из DataStore происходит асинхронно.

**Результат:** Метод `refreshUserOwnReviews()` не вызывался, так как `isAuthenticated.value = false`.

**Решение:** Добавлена реактивность - `LaunchedEffect(isAuthenticated)` отслеживает изменение состояния аутентификации и перезагружает отзывы когда пользователь становится авторизованным.

## Корректная логика отображения (Flow)

### Сценарий 1: Отправка отзыва
1. Пользователь заполняет форму и отправляет отзыв
2. **Сразу же** отзыв появляется в секции "Ваш отзыв" с бейджем "🟡 На модерации"
3. Отзыв НЕ появляется в списке "Отзывы других пользователей"

### Сценарий 2: Одобрение отзыва
1. Администратор меняет статус на `approved`
2. Отзыв **остается** в секции "Ваш отзыв", но бейдж статуса исчезает
3. Отзыв **НЕ дублируется** в список "Отзывы других пользователей"
4. Другие пользователи видят этот отзыв в публичном списке

### Сценарий 3: Отклонение отзыва
1. Администратор меняет статус на `rejected`
2. Отзыв остается в секции "Ваш отзыв" с бейджем "🔴 Отклонён"
3. Отзыв НЕ виден другим пользователям

### Секция "Ваш отзыв" / "Ваши отзывы"
- Отображается выше всех других отзывов
- Содержит отзывы текущего пользователя с **любым статусом**:
  - 🟡 `pending` - На модерации (с бейджем)
  - 🟢 `approved` - Опубликован (БЕЗ бейджа)
  - 🔴 `rejected` - Отклонён (с бейджем)
- Отзывы видны **сразу после отправки**

### Секция "Отзывы других пользователей"
- Отображается ниже секции с отзывами пользователя
- Содержит **только одобренные отзывы** (`status = 'approved'`) других пользователей
- **НЕ содержит** отзывы текущего пользователя (избегается дублирование)

## Внесенные изменения

### 1. ReviewRepository.kt

**Файл:** `AdyhyesKOTLIN/app/src/main/java/com/adygyes/app/data/repository/ReviewRepository.kt`

#### Изменение 1: Добавлен параметр `excludeUserId` в метод `refreshApprovedReviews`

```kotlin
suspend fun refreshApprovedReviews(attractionId: String, excludeUserId: String? = null) {
    when (val result = reviewsRemoteDataSource.getApprovedReviews(attractionId)) {
        is NetworkResult.Success -> {
            val currentUserId = getCurrentUserId()
            val base = result.data
                // КРИТИЧНО: исключаем отзывы текущего пользователя из публичного списка
                .filter { dto -> excludeUserId == null || dto.userId != excludeUserId }
                .map { dto ->
                    // ... mapping logic
                }
            // ...
        }
    }
}
```

**Зачем:** Фильтрация на уровне репозитория гарантирует, что отзывы пользователя не попадут в публичный список даже если они имеют статус `approved`.

#### Изменение 2: Сделан метод `getCurrentUserId()` публичным

```kotlin
fun getCurrentUserId(): String? {
    val authState = authRepository.authState.value
    return (authState as? AuthState.Authenticated)?.user?.id
}
```

**Зачем:** ViewModel теперь может получить ID текущего пользователя для передачи в `refreshApprovedReviews`.

#### ✅ Уже работает: `submitReview` добавляет отзыв в кэш

```kotlin
// Add to user's own reviews cache
_userOwnReviews.value = listOf(review) + _userOwnReviews.value
```

**Это важно:** После создания отзыва он сразу добавляется в локальный кэш `_userOwnReviews`.

### 2. ReviewViewModel.kt

**Файл:** `AdyhyesKOTLIN/app/src/main/java/com/adygyes/app/presentation/viewmodel/ReviewViewModel.kt`

#### Изменение 1: Правильный порядок загрузки и фильтрации

```kotlin
viewModelScope.launch {
    try {
        checkUserReviewed(attractionId)
        
        // КРИТИЧНО: сначала загружаем отзывы пользователя
        if (isAuthenticated.value) {
            reviewRepository.refreshUserOwnReviews(attractionId)
            _userOwnReviews.value = reviewRepository.userOwnReviews.value
            Timber.d("Loaded user own reviews, count=${_userOwnReviews.value.size}")
        } else {
            _userOwnReviews.value = emptyList()
        }
        
        // Получаем ID пользователя для фильтрации
        val currentUserId = if (isAuthenticated.value) {
            reviewRepository.getCurrentUserId()
        } else null
        
        // Загружаем публичные отзывы с исключением отзывов пользователя
        reviewRepository.refreshApprovedReviews(attractionId, excludeUserId = currentUserId)
        reviewRepository.getReviewsForAttraction(attractionId, _sortBy.value)
            .collect { allReviews ->
                // Дополнительная фильтрация для безопасности
                val userReviewIds = _userOwnReviews.value.map { it.id }.toSet()
                _reviews.value = allReviews.filter { it.id !in userReviewIds }
                _loading.value = false
            }
    }
    // ...
}
```

**Зачем:** 
1. Гарантирует загрузку отзывов пользователя **до** загрузки публичных отзывов
2. Передает `excludeUserId` в репозиторий для фильтрации на уровне данных
3. Добавляет дополнительную фильтрацию по ID для защиты от дублирования

#### Изменение 2: **КРИТИЧНО** - Обновление локального стейта после отправки отзыва

```kotlin
fun submitReview(attractionId: String, rating: Int, text: String?) {
    viewModelScope.launch {
        // ...
        reviewRepository.submitReview(submission)
            .onSuccess { newReview ->
                Timber.d("Successfully submitted review, status=${newReview.status}")
                
                // КРИТИЧНО: обновить локальный стейт сразу с новым отзывом
                // Репозиторий уже добавил отзыв в свой кэш, копируем его
                _userOwnReviews.value = reviewRepository.userOwnReviews.value
                
                // Обновить hasUserReviewed флаг
                _hasUserReviewed.value = true
                
                // Обновить публичные отзывы
                val currentUserId = reviewRepository.getCurrentUserId()
                reviewRepository.refreshApprovedReviews(attractionId, excludeUserId = currentUserId)
                
                _submitSuccess.value = true
                
                Timber.d("User own reviews count after submit: ${_userOwnReviews.value.size}")
            }
    }
}
```

**Зачем:** 
- **Главное исправление!** Сразу после успешной отправки обновляем `_userOwnReviews` из репозитория
- Репозиторий уже добавил новый отзыв в свой кэш `_userOwnReviews`
- UI сразу отобразит новый отзыв в секции "Ваш отзыв" с бейджем "На модерации"
- Не вызываем `loadReviews()`, чтобы избежать race condition

#### Изменение 3: Обновление userOwnReviews в кэшированном пути

```kotlin
if (!forceRefresh && currentAttractionId == attractionId && _reviews.value.isNotEmpty()) {
    // Already loaded, но всё равно обновим userOwnReviews на всякий случай
    if (isAuthenticated.value) {
        viewModelScope.launch {
            reviewRepository.refreshUserOwnReviews(attractionId)
            _userOwnReviews.value = reviewRepository.userOwnReviews.value
            Timber.d("Refreshed user own reviews (cached path)")
        }
    }
    return
}
```

**Зачем:** Даже если публичные отзывы закэшированы, обновляем отзывы пользователя (статус мог измениться).

### 3. ReviewSection.kt

**Файл:** `AdyhyesKOTLIN/app/src/main/java/com/adygyes/app/presentation/ui/components/reviews/ReviewSection.kt`

#### Изменение 1: Улучшен заголовок секции

```kotlin
Text(
    text = if (userOwnReviews.size == 1) "Ваш отзыв" else "Ваши отзывы",
    // ...
)
```

**Зачем:** Правильная грамматика (единственное/множественное число).

#### Изменение 2: Условное отображение статуса

```kotlin
userOwnReviews.forEach { review ->
    Column(
        verticalArrangement = Arrangement.spacedBy(8.dp)
    ) {
        // Показываем статус только если он не "approved" или null
        if (review.status != null && review.status != "approved") {
            StatusBadge(status = review.status)
        }
        ReviewCard(
            review = review,
            onLike = onLike,
            onDislike = onDislike,
            onShare = onShare
        )
    }
}
```

**Зачем:** 
- Бейдж статуса скрыт для опубликованных отзывов (это очевидно)
- Отображается только для `pending` (🟡 На модерации) и `rejected` (🔴 Отклонён)

## Результат

### ✅ Что исправлено

1. **Отзыв появляется СРАЗУ после отправки** в секции "Ваш отзыв" с бейджем "На модерации"
2. **Отзыв пользователя больше не дублируется** в списке отзывов других людей
3. **Статус отображается корректно**: только для pending/rejected
4. **Порядок загрузки гарантирован**: сначала отзывы пользователя, потом публичные
5. **Трёхуровневая защита от дублирования**: 
   - Репозиторий фильтрует по `excludeUserId`
   - ViewModel фильтрует по ID отзывов
   - UI разделяет на две секции
6. **Правильная грамматика**: "Ваш отзыв" vs "Ваши отзывы"
7. **Обновление локального стейта** после отправки отзыва

### Детальные сценарии работы

#### Сценарий 1: Пользователь оставил отзыв (pending)
**Действие:** Пользователь заполнил форму и нажал "Отправить"

**Ожидаемое поведение:**
- ✅ Отзыв **сразу** появляется в секции "Ваш отзыв"
- ✅ Над отзывом отображается бейдж "🟡 На модерации"
- ✅ Секция "Отзывы других пользователей" содержит только отзывы других людей
- ✅ Отзыв пользователя НЕ дублируется

**Техническая реализация:**
1. `submitReview()` создает отзыв через API
2. Репозиторий добавляет отзыв в `_userOwnReviews`
3. ViewModel копирует: `_userOwnReviews.value = reviewRepository.userOwnReviews.value`
4. UI немедленно отображает отзыв

#### Сценарий 2: Отзыв одобрен (approved)
**Действие:** Админ меняет статус на `approved`

**Ожидаемое поведение:**
- ✅ Отзыв **остается** в секции "Ваш отзыв"
- ✅ Бейдж статуса **исчезает** (статус `approved` очевиден)
- ✅ Отзыв **НЕ появляется** в секции "Отзывы других пользователей"
- ✅ Другие пользователи видят этот отзыв в публичном списке

**Техническая реализация:**
1. `refreshUserOwnReviews()` загружает отзыв со статусом `approved`
2. `refreshApprovedReviews(excludeUserId = currentUserId)` исключает отзыв из публичного списка
3. UI проверяет: `if (review.status != "approved")` → бейдж не показывается

#### Сценарий 3: Отзыв отклонен (rejected)
**Действие:** Админ меняет статус на `rejected`

**Ожидаемое поведение:**
- ✅ Отзыв остается в секции "Ваш отзыв"
- ✅ Отображается бейдж "🔴 Отклонён"
- ✅ Опционально: причина отклонения (если заполнена)
- ✅ Отзыв НЕ виден другим пользователям

#### Сценарий 4: Пользователь не авторизован
**Ожидаемое поведение:**
- ✅ Секция "Ваш отзыв" не отображается
- ✅ Секция "Отзывы других пользователей" показывает все публичные отзывы

#### Сценарий 5: Обновление данных (Pull to Refresh)
**Действие:** Пользователь обновляет страницу

**Ожидаемое поведение:**
- ✅ Отзывы пользователя обновляются (может измениться статус)
- ✅ Публичные отзывы обновляются
- ✅ Дублирование не происходит благодаря фильтрации

## Связанные файлы

### Измененные:
- ✅ `ReviewRepository.kt` - добавлен параметр `excludeUserId`, публичный `getCurrentUserId()`
- ✅ `ReviewViewModel.kt` - исправлена логика `submitReview()`, улучшен `loadReviews()`
- ✅ `ReviewSection.kt` - условное отображение бейджа статуса

### Без изменений:
- ✅ `StatusBadge.kt` - компонент бейджа статуса (работает корректно)
- ✅ `ReviewCard.kt` - карточка отзыва
- ✅ `Review.kt` - модель данных
- ✅ `SupabaseApiService.kt` - API эндпоинты (работают корректно)

## Тестирование

### Проверить вручную:

#### Тест 1: Отправка отзыва
1. Войти в аккаунт
2. Открыть карточку достопримечательности
3. Нажать "Оставить отзыв", заполнить форму, отправить
4. **✅ ПРОВЕРКА:** Отзыв сразу появился в секции "Ваш отзыв" с бейджем "На модерации"
5. **✅ ПРОВЕРКА:** Отзыв НЕ появился в списке "Отзывы других пользователей"

#### Тест 2: Одобрение отзыва
1. Из админки изменить статус на `approved`
2. В приложении обновить данные (pull to refresh или переоткрыть)
3. **✅ ПРОВЕРКА:** Отзыв остался в секции "Ваш отзыв"
4. **✅ ПРОВЕРКА:** Бейдж статуса исчез
5. **✅ ПРОВЕРКА:** Отзыв НЕ дублируется в общем списке
6. Выйти из аккаунта или открыть с другого устройства
7. **✅ ПРОВЕРКА:** Отзыв виден в публичном списке

#### Тест 3: Отклонение отзыва
1. Из админки изменить статус на `rejected` с причиной
2. В приложении обновить данные
3. **✅ ПРОВЕРКА:** Отзыв остался в секции "Ваш отзыв"
4. **✅ ПРОВЕРКА:** Отображается бейдж "Отклонён"
5. **✅ ПРОВЕРКА:** Отзыв НЕ виден другим пользователям

#### Тест 4: Логи
Проверить логи Timber:
```
Successfully submitted review, status=pending
User own reviews count after submit: 1
Loaded user own reviews, count=1, statuses=[pending]
```

## Примечания

- Фильтрация работает на **трёх уровнях** для максимальной надежности
- Статус `approved` считается по умолчанию, поэтому бейдж не нужен
- Логика полностью унифицирована с React Native версией приложения
- Добавлены логи Timber для отладки
- Исправлен критический баг с отображением отзывов сразу после отправки

## Внесенные изменения

### 1. ReviewRepository.kt

**Файл:** `AdyhyesKOTLIN/app/src/main/java/com/adygyes/app/data/repository/ReviewRepository.kt`

#### Изменение 1: Добавлен параметр `excludeUserId` в метод `refreshApprovedReviews`

```kotlin
suspend fun refreshApprovedReviews(attractionId: String, excludeUserId: String? = null) {
    when (val result = reviewsRemoteDataSource.getApprovedReviews(attractionId)) {
        is NetworkResult.Success -> {
            val currentUserId = getCurrentUserId()
            val base = result.data
                // КРИТИЧНО: исключаем отзывы текущего пользователя из публичного списка
                .filter { dto -> excludeUserId == null || dto.userId != excludeUserId }
                .map { dto ->
                    // ... mapping logic
                }
            // ...
        }
    }
}
```

**Зачем:** Фильтрация на уровне репозитория гарантирует, что отзывы пользователя не попадут в публичный список даже если они имеют статус `approved`.

#### Изменение 2: Сделан метод `getCurrentUserId()` публичным

```kotlin
fun getCurrentUserId(): String? {
    val authState = authRepository.authState.value
    return (authState as? AuthState.Authenticated)?.user?.id
}
```

**Зачем:** ViewModel теперь может получить ID текущего пользователя для передачи в `refreshApprovedReviews`.

### 2. ReviewViewModel.kt

**Файл:** `AdyhyesKOTLIN/app/src/main/java/com/adygyes/app/presentation/viewmodel/ReviewViewModel.kt`

#### Изменение: Правильный порядок загрузки и фильтрации

```kotlin
viewModelScope.launch {
    try {
        checkUserReviewed(attractionId)
        
        // КРИТИЧНО: сначала загружаем отзывы пользователя
        if (isAuthenticated.value) {
            reviewRepository.refreshUserOwnReviews(attractionId)
            _userOwnReviews.value = reviewRepository.userOwnReviews.value
        } else {
            _userOwnReviews.value = emptyList()
        }
        
        // Получаем ID пользователя для фильтрации
        val currentUserId = if (isAuthenticated.value) {
            reviewRepository.getCurrentUserId()
        } else null
        
        // Загружаем публичные отзывы с исключением отзывов пользователя
        reviewRepository.refreshApprovedReviews(attractionId, excludeUserId = currentUserId)
        reviewRepository.getReviewsForAttraction(attractionId, _sortBy.value)
            .collect { allReviews ->
                // Дополнительная фильтрация для безопасности
                val userReviewIds = _userOwnReviews.value.map { it.id }.toSet()
                _reviews.value = allReviews.filter { it.id !in userReviewIds }
                _loading.value = false
            }
    }
    // ...
}
```

**Зачем:** 
1. Гарантирует загрузку отзывов пользователя **до** загрузки публичных отзывов
2. Передает `excludeUserId` в репозиторий для фильтрации на уровне данных
3. Добавляет дополнительную фильтрацию по ID для защиты от дублирования

### 3. ReviewSection.kt

**Файл:** `AdyhyesKOTLIN/app/src/main/java/com/adygyes/app/presentation/ui/components/reviews/ReviewSection.kt`

#### Изменение 1: Улучшен заголовок секции

```kotlin
Text(
    text = if (userOwnReviews.size == 1) "Ваш отзыв" else "Ваши отзывы",
    // ...
)
```

**Зачем:** Правильная грамматика (единственное/множественное число).

#### Изменение 2: Условное отображение статуса

```kotlin
userOwnReviews.forEach { review ->
    Column(
        verticalArrangement = Arrangement.spacedBy(8.dp)
    ) {
        // Показываем статус только если он не "approved" или null
        if (review.status != null && review.status != "approved") {
            StatusBadge(status = review.status)
        }
        ReviewCard(
            review = review,
            onLike = onLike,
            onDislike = onDislike,
            onShare = onShare
        )
    }
}
```

**Зачем:** 
- Бейдж статуса скрыт для опубликованных отзывов (это очевидно)
- Отображается только для `pending` (🟡 На модерации) и `rejected` (🔴 Отклонён)

## Результат

### ✅ Что исправлено

1. **Отзыв пользователя больше не дублируется** в списке отзывов других людей
2. **Статус отображается корректно**: только для pending/rejected
3. **Порядок загрузки гарантирован**: сначала отзывы пользователя, потом публичные
4. **Двухуровневая фильтрация**: на уровне репозитория и ViewModel
5. **Правильная грамматика**: "Ваш отзыв" vs "Ваши отзывы"

### Сценарии работы

#### Сценарий 1: Пользователь оставил отзыв, он на модерации
- ✅ Секция "Ваш отзыв" содержит отзыв с бейджем "🟡 На модерации"
- ✅ Секция "Отзывы других пользователей" содержит только отзывы других людей
- ✅ Отзыв пользователя НЕ дублируется

#### Сценарий 2: Отзыв пользователя опубликован
- ✅ Секция "Ваш отзыв" содержит отзыв БЕЗ бейджа статуса
- ✅ Секция "Отзывы других пользователей" содержит только отзывы других людей
- ✅ Отзыв пользователя НЕ появляется в общем списке

#### Сценарий 3: Отзыв пользователя отклонён
- ✅ Секция "Ваш отзыв" содержит отзыв с бейджем "🔴 Отклонён"
- ✅ Секция "Отзывы других пользователей" содержит только отзывы других людей
- ✅ Отзыв пользователя НЕ виден другим пользователям

#### Сценарий 4: Пользователь не авторизован
- ✅ Секция "Ваш отзыв" не отображается
- ✅ Секция "Отзывы других пользователей" показывает все публичные отзывы

## Связанные файлы

- ✅ `ReviewRepository.kt` - репозиторий с логикой загрузки и фильтрации
- ✅ `ReviewViewModel.kt` - ViewModel с правильным порядком загрузки
- ✅ `ReviewSection.kt` - UI компонент с условным отображением статуса
- ✅ `StatusBadge.kt` - компонент бейджа статуса (без изменений)
- ✅ `Review.kt` - модель данных (без изменений)

## Тестирование

### Проверить вручную:

1. Войти в аккаунт
2. Оставить отзыв для достопримечательности
3. **Проверка 1:** Отзыв должен появиться в секции "Ваш отзыв" с бейджем "На модерации"
4. **Проверка 2:** Отзыв НЕ должен появиться в списке "Отзывы других пользователей"
5. Из админки изменить статус на `approved`
6. **Проверка 3:** Обновить данные в приложении
7. **Проверка 4:** Отзыв должен остаться в секции "Ваш отзыв" БЕЗ бейджа
8. **Проверка 5:** Отзыв НЕ должен дублироваться в общем списке

## Примечания

- Фильтрация работает на **двух уровнях** для максимальной надежности
- Статус `approved` считается по умолчанию, поэтому бейдж не нужен
- Логика полностью унифицирована с React Native версией приложения
